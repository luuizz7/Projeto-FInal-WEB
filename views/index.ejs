<%- include('partials/header') %>

<main class="container mt-5">
    <div class="bg-light p-5 rounded-lg m-3 text-center">
        <h1 class="display-4">Bem-vindo ao Sistema Escolar!</h1>
        <p class="lead">Gerencie alunos e turmas de forma eficiente e organizada.</p>
        <hr class="my-4">
        <p>Utilize a barra de navegação para acessar as funcionalidades do sistema.</p>
        <% if (session.userId) { %>
          <a class="btn btn-primary btn-lg" href="/alunos" role="button">Ver Alunos</a>
        <% } else { %>
          <a class="btn btn-success btn-lg" href="/auth/login" role="button">Fazer Login</a>
        <% } %>
    </div>
</main>

<%- include('partials/footer') %>